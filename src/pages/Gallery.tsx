import {useEffect,useState} from 'react';import {AnimatePresence,motion} from 'framer-motion';import {Camera,ChevronLeft,ChevronRight,X,ZoomIn,Star,Quote} from 'lucide-react';import {galleryItems,testimonials} from '../data/content';
const filters=['All','International','Domestic','Family','Devotional','Honeymoon'];
export default function Gallery(){const [filter,setFilter]=useState('All'),[selected,setSelected]=useState<number|null>(null);const items=galleryItems.filter(x=>filter==='All'||x.category===filter);useEffect(()=>{const key=(e:KeyboardEvent)=>{if(e.key==='Escape')setSelected(null);if(selected!==null&&e.key==='ArrowRight')setSelected((selected+1)%items.length);if(selected!==null&&e.key==='ArrowLeft')setSelected((selected-1+items.length)%items.length)};addEventListener('keydown',key);return()=>removeEventListener('keydown',key)},[selected,items.length]);return <div className="bg-[#f7f8fb]"><section className="relative flex min-h-[420px] items-end overflow-hidden bg-navy pb-16 text-white"><img src="/images/hero/europe.jpg" alt="Kakani Holidays travel gallery" className="absolute inset-0 h-full w-full object-cover opacity-50"/><div className="absolute inset-0 bg-gradient-to-r from-navy via-navy/65 to-transparent"/><div className="lux-container relative"><p className="lux-eyebrow !text-[#ffc45c]">Gallery & reviews</p><h1 className="mt-4 font-serif text-5xl md:text-7xl">Memories made<br/>around the world.</h1></div></section><section className="py-16"><div className="lux-container"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="lux-eyebrow">Travel gallery</p><h2 className="mt-3 font-serif text-4xl md:text-6xl">Moments worth keeping.</h2></div><div className="no-scrollbar flex max-w-full gap-2 overflow-x-auto pb-1">{filters.map(x=><button key={x} onClick={()=>setFilter(x)} className={`whitespace-nowrap rounded-full px-4 py-2 text-xs font-bold transition ${filter===x?'bg-navy text-white':'bg-white text-slate-600 shadow-sm'}`}>{x}</button>)}</div></div><motion.div layout className="mt-10 columns-1 gap-4 sm:columns-2 lg:columns-3">{items.map((x,i)=><motion.button layout initial={{opacity:0,y:15}} animate={{opacity:1,y:0}} key={x.id} onClick={()=>setSelected(i)} className="group relative mb-4 block w-full break-inside-avoid overflow-hidden rounded-[24px] text-left shadow-sm"><img src={x.image} alt={x.imageLabel} loading="lazy" className={`w-full object-cover transition duration-700 group-hover:scale-105 ${x.shape==='portrait'?'h-[430px]':x.shape==='landscape'?'h-[280px]':'h-[340px]'}`}/><div className="absolute inset-0 bg-gradient-to-t from-navy/85 via-transparent to-transparent opacity-80 transition group-hover:opacity-100"/><div className="absolute inset-x-0 bottom-0 flex items-end justify-between p-5 text-white"><div><span className="text-[9px] font-bold uppercase tracking-widest text-[#ffc45c]">{x.category}</span><p className="mt-1 font-serif text-xl">{x.imageLabel}</p></div><span className="grid h-9 w-9 place-items-center rounded-full bg-white/15 backdrop-blur"><ZoomIn size={16}/></span></div></motion.button>)}</motion.div></div></section><section className="bg-white py-16"><div className="lux-container"><div className="text-center"><p className="lux-eyebrow">Traveller reviews</p><h2 className="mt-3 font-serif text-4xl md:text-6xl">Words from the journey.</h2></div><div className="mt-10 grid gap-5 md:grid-cols-3">{testimonials.map(t=><article key={t.name} className="rounded-[24px] bg-[#f7f8fb] p-6"><Quote className="text-sunset"/><div className="mt-4 flex text-[#e5a52e]">{[1,2,3,4,5].map(x=><Star key={x} size={13} fill="currentColor"/>)}</div><p className="mt-4 text-sm leading-7 text-slate-600">{t.review}</p><p className="mt-5 text-sm font-bold text-navy">{t.name}<span className="block text-xs font-normal text-slate-400">{t.destination}</span></p></article>)}</div></div></section><AnimatePresence>{selected!==null&&items[selected]&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-[100] grid place-items-center bg-navy/95 p-4" onClick={()=>setSelected(null)}><button className="absolute right-4 top-4 grid h-11 w-11 place-items-center rounded-full bg-white text-navy" aria-label="Close gallery"><X/></button><button onClick={e=>{e.stopPropagation();setSelected((selected-1+items.length)%items.length)}} className="absolute left-3 grid h-10 w-10 place-items-center rounded-full bg-white/15 text-white"><ChevronLeft/></button><img onClick={e=>e.stopPropagation()} src={items[selected].image} alt={items[selected].imageLabel} className="max-h-[85vh] max-w-[90vw] rounded-2xl object-contain shadow-2xl"/><button onClick={e=>{e.stopPropagation();setSelected((selected+1)%items.length)}} className="absolute right-3 grid h-10 w-10 place-items-center rounded-full bg-white/15 text-white"><ChevronRight/></button></motion.div>}</AnimatePresence></div>}
